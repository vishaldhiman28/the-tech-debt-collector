FROM golang:1.21-alpine AS builder

WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

COPY . .

RUN CGO_ENABLED=0 go build -o dashboard ./web/main.go

FROM alpine:latest

WORKDIR /app

COPY --from=builder /app/dashboard .
COPY web/static ./static

EXPOSE 8080

CMD ["./dashboard"]
